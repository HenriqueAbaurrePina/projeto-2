apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-fresh
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: azurefile
